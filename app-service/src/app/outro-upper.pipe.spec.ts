import { OutroUpperPipe } from './outro-upper.pipe';

describe('OutroUpperPipe', () => {
  it('create an instance', () => {
    const pipe = new OutroUpperPipe();
    expect(pipe).toBeTruthy();
  });
});
